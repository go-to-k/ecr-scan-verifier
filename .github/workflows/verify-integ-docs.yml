# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: verify-integ-docs
on:
  pull_request: {}
  workflow_dispatch: {}
jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Run verify-integ-docs
        run: ./scripts/verify-integ-docs.sh
